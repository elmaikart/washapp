"use client";

import React from "react";

export default function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 h-16 bg-wash-bg shadow-md z-50 flex items-center justify-center">
      <h1 className="text-xl font-bold text-wash-primary">WashApp</h1>
    </header>

  );
}
